This file is never rendered; it is required to allow the setup.do action as
the welcome-file in web.xml.

This file should have the name of the action we want to execute for a
url with no path component. A file of this name must exist but will
not be used (except to fool the container).

